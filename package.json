{
  "name": "farol-app-baixar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --assets ./dist"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
